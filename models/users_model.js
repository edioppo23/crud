// import { resolveSoa } from "dns"

// Schema:
// name, [type:string, required ]
// email, [type: string, unique, emailformat, required]
// username, [type:  string, unique, lower case, snake case, required]
// phone, [type: string, unique, required]
// gender, [type: string, nullable]
// password, [type: string, encrypted, required]
// activated_at, [type: date, nullable]
// activation_token, [type: string, random alpha numeric, nullable]
// roles, [type: array, list of role_id, reference: Roles, required]
// created_at, [type: date],
// updated_at, [type : date],
// deleted_at, [type: date, nullable] 

